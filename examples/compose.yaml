services:
  jaeger:
    image: cr.jaegertracing.io/jaegertracing/jaeger:2.21.0
    ports:
      - "127.0.0.1:16686:16686"
      - "127.0.0.1:4318:4318"
    # Development only: in-memory trace storage, no authentication.
    # No privileged mode, host PID namespace or runtime sensors required.
